function calculateArea(l: number, width: number): number {
    return l * width;
}

// Inputs
let l: number = 5;
let width: number = 3;

// Calculate area
let area: number = calculateArea(l, width);

// Output the result
console.log("The area of the rectangle is:", area);